<%- include('include/_header') %>

<main id="site-main">
    <div class="container">
        <div class="box-nav d-flex justify-between">
            <div class="filter">
                <a href="/" class="btn btn-primary">  listagem </a>
            </div>
        </div>
        <div class="form-title text-center">
            <h2 class="text-dark">Atualizar Usuário</h2>
        </div>

        <form method="POST" id="update_user">
            <div class="new_user">
                <div class="form-group">
                    <label for="name" class="text-light">Nome</label>
                    <input type="hidden" name="id" value="<%= user._id %>" />
                    <input
                        type="text"
                        name="name"
                        value="<%= user.name %>"
                        placeholder="Seu nome"
                    />
                </div>
                <div class="form-group">
                    <label for="Email" class="text-light">E-mail</label>
                    <input
                        type="text"
                        name="email"
                        value="<%= user.email%>"
                        placeholder="Seu e-mail"
                    />
                </div>
                <div class="form-group">
                    <label for="sexo" class="text-light">Sexo</label>
                    <div class="radio inline">
                        <input type="radio" id="radio-2" name="sexo"
                        value="Masculino" <%= user.sexo == 'Masculino' ?
                        'checked' : '' %>>
                        <label for="radio-2" class="radio-label"
                            >Masculino</label
                        >
                    </div>
                    <div class="radio inline">
                        <input type="radio" id="radio-3" name="sexo"
                        value="Feminino" <%= user.sexo == 'Feminino' ? 'checked'
                        : '' %> >
                        <label for="radio-3" class="radio-label"
                            >Feminino</label
                        >
                    </div>
                </div>

                <div class="form-group">
                    <label for="gender" class="text-light">Status</label>
                    <div class="radio inline">
                        <input type="radio" id="radio-4" name="status"
                        value="Ativo" <%= user.status == 'Ativo' ? 'checked' :
                        '' %> >
                        <label for="radio-4" class="radio-label">Ativo</label>
                    </div>
                    <div class="radio inline">
                        <input type="radio" id="radio-5" name="status"
                        value="Inativo" <%= user.status == 'Inativo' ? 'checked'
                        : '' %> >
                        <label for="radio-5" class="radio-label">Inativo</label>
                    </div>
                </div>

                <div class="d-grid gap-2">
                    <button class="btn btn-success update" type="submit">Salvar</button>
                </div>
                </div>
            </div>
        </form>
    </div>
</main>

<%- include('include/_footer') %>
